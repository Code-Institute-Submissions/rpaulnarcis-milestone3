 $(document).ready(function(){
    $('.sidenav').sidenav({edge: "right"});
    $('select').formSelect()
     $('.carousel').carousel({
          full_width: true
      });
 });

 function showHeight( element, height ) {
$( "div.what-height" ).text( "The height for the " + element + " is " + height + "px." );
}
$( "div.first" ).click(function() {
showHeight( "card-panel", $( "div.fist" ).height(20) );
});